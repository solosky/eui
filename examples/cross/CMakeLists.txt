# examples/cross/CMakeLists.txt
# Cross-platform examples — linked against BOOTSTRAP_LIB based on EUI_TARGET_PORT
add_subdirectory(basic_label)
add_subdirectory(button_test)
add_subdirectory(list_nav)
